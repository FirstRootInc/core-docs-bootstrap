---
id: accepting-filters-enum
title: AcceptingFiltersEnum
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AcceptingFiltersEnum {
  CONFIRMED
  NOT_CONFIRMED
  ALL
}
```

### Values

#### `CONFIRMED`

#### `NOT_CONFIRMED`

#### `ALL`
