---
id: reactions-enum
title: ReactionsEnum
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ReactionsEnum {
  LIKE
  DISLIKE
  NEUTRAL
}
```

### Values

#### `LIKE`

#### `DISLIKE`

#### `NEUTRAL`
