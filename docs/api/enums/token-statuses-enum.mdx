---
id: token-statuses-enum
title: TokenStatusesEnum
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum TokenStatusesEnum {
  NOT_FOUND
  USED
  NOT_YET_ACTIVE
  EXPIRED
  VALID
}
```

### Values

#### `NOT_FOUND`

#### `USED`

#### `NOT_YET_ACTIVE`

#### `EXPIRED`

#### `VALID`
