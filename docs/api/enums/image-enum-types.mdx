---
id: image-enum-types
title: ImageEnumTypes
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ImageEnumTypes {
  JPG
  GIF
  PNG
}
```

### Values

#### `JPG`

A JPG file

#### `GIF`

A GIF file

#### `PNG`

A PNG file
