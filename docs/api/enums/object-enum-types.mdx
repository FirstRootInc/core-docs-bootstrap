---
id: object-enum-types
title: ObjectEnumTypes
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ObjectEnumTypes {
  PROPOSAL
  COMMENT
  PBCYCLE
  School
}
```

### Values

#### `PROPOSAL`

Proposal object

#### `COMMENT`

Comment object

#### `PBCYCLE`

PB Cycle object

#### `School`

School object
