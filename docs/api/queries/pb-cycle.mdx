---
id: pb-cycle
title: pbCycle
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Find a PB cycle by UUID

```graphql
pbCycle(
  id: ID!
): PBCycle!

```

### Arguments

#### `id` ([`ID!`](/docs/api/scalars/id))

### Type

#### [`PBCycle`](/docs/api/objects/pb-cycle)
