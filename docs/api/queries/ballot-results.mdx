---
id: ballot-results
title: ballotResults
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Get ballot results

```graphql
ballotResults(
  pbCycleId: ID!
): BallotResult

```

### Arguments

#### `pbCycleId` ([`ID!`](/api/scalars/id))

### Type

#### [`BallotResult`](/api/objects/ballot-result)
