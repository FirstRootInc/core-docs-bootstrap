---
id: pb-invite-tokens
title: pbInviteTokens
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Lists all the PB Invitation tokens

```graphql
pbInviteTokens(
  pbCycleId: ID!
  type: TokenTypesEnum
): [VerificationToken!]

```

### Arguments

#### `pbCycleId` ([`ID!`](/api/scalars/id))

#### `type` ([`TokenTypesEnum`](/api/enums/token-types-enum))

### Type

#### [`VerificationToken`](/api/objects/verification-token)
