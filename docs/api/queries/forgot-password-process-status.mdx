---
id: forgot-password-process-status
title: forgotPasswordProcessStatus
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Checks the status for forgot password token

```graphql
forgotPasswordProcessStatus(
  token: String!
): ForgotTokenStatus!

```

### Arguments

#### `token` ([`String!`](/api/scalars/string))

The forgot password token received by user via email.

### Type

#### [`ForgotTokenStatus`](/api/objects/forgot-token-status)
