---
id: users
title: users
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Find all users

```graphql
users: [User!]

```

### Type

#### [`User`](/api/objects/user)
