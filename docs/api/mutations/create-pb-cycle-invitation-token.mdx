---
id: create-pb-cycle-invitation-token
title: createPbCycleInvitationToken
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Allow to create a new invitation token for PB cycles. PB cycles can have only one invitation active, so if the PB cycle has an invitation already created this mutation will return an error.

```graphql
createPbCycleInvitationToken(
  input: CreatePBCycleInvitationTokenInput!
): CreatePBCycleInvitationTokenPayload

```

### Arguments

#### `input` ([`CreatePBCycleInvitationTokenInput!`](/docs/api/inputs/create-pb-cycle-invitation-token-input))

### Type

#### [`CreatePBCycleInvitationTokenPayload`](/docs/api/objects/create-pb-cycle-invitation-token-payload)

Autogenerated return type of CreatePBCycleInvitationToken
