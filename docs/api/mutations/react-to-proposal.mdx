---
id: react-to-proposal
title: reactToProposal
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Allows to react to a proposal

```graphql
reactToProposal(
  input: ReactToProposalInput!
): ReactToProposalPayload

```

### Arguments

#### `input` ([`ReactToProposalInput!`](/api/inputs/react-to-proposal-input))

### Type

#### [`ReactToProposalPayload`](/api/objects/react-to-proposal-payload)

Autogenerated return type of ReactToProposal
