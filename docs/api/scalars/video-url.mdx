---
id: video-url
title: VideoUrl
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Video URL scalar represents a URL pointing to a video. The only type of video URL this scalar accepts right is a YouTube video URL.

```graphql
scalar VideoUrl
```
