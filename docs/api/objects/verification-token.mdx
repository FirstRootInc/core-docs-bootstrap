---
id: verification-token
title: VerificationToken
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VerificationToken {
  category: String!
  createdAt: ISO8601DateTime!
  id: ID!
  singleUse: Boolean!
  token: String!
  updatedAt: ISO8601DateTime!
  usedAt: ISO8601DateTime
  validFrom: ISO8601DateTime
  validUntil: ISO8601DateTime
  verifiableId: ID!
  verifiableType: String!
}
```

### Fields

#### `category` ([`String!`](/api/scalars/string))

Category for the verification token.

#### `createdAt` ([`ISO8601DateTime!`](/api/scalars/iso-8601-date-time))

#### `id` ([`ID!`](/api/scalars/id))

#### `singleUse` ([`Boolean!`](/api/scalars/boolean))

Indicates if this verification token has been created to be uses only once.

#### `token` ([`String!`](/api/scalars/string))

The code or _token_ that will be used to verify the action.

#### `updatedAt` ([`ISO8601DateTime!`](/api/scalars/iso-8601-date-time))

#### `usedAt` ([`ISO8601DateTime`](/api/scalars/iso-8601-date-time))

#### `validFrom` ([`ISO8601DateTime`](/api/scalars/iso-8601-date-time))

The initial value in which this verification token starts to be valid.

#### `validUntil` ([`ISO8601DateTime`](/api/scalars/iso-8601-date-time))

The final date and time in which this token still be valid.

#### `verifiableId` ([`ID!`](/api/scalars/id))

The related entity ID. This is the entity that uses the token to verify a certain action

#### `verifiableType` ([`String!`](/api/scalars/string))

The related entity type.
