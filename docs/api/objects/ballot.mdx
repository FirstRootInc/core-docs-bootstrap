---
id: ballot
title: Ballot
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type Ballot {
  budget: Decimal!
  createdAt: ISO8601Date!
  id: ID!
  partyBallots: [PartyBallot!]
  pbCycle: PBCycle!
  proposals: [BallotProposal!]
  updatedAt: ISO8601Date!
}
```

### Fields

#### `budget` ([`Decimal!`](/api/scalars/decimal))

#### `createdAt` ([`ISO8601Date!`](/api/scalars/iso-8601-date))

#### `id` ([`ID!`](/api/scalars/id))

#### `partyBallots` ([`[PartyBallot!]`](/api/objects/party-ballot))

#### `pbCycle` ([`PBCycle!`](/api/objects/pb-cycle))

PB Cycle which the ballot belongs to

#### `proposals` ([`[BallotProposal!]`](/api/objects/ballot-proposal))

List of proposals included in this ballot

#### `updatedAt` ([`ISO8601Date!`](/api/scalars/iso-8601-date))
