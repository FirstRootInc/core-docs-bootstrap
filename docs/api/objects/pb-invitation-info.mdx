---
id: pb-invitation-info
title: PBInvitationInfo
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PBInvitationInfo {
  acceptedInvitations: PBAcceptedInvitationsStatistics
  invitationsTokens: [VerificationToken!]!
  pbCycle: PBCycle!
}
```

### Fields

#### `acceptedInvitations` ([`PBAcceptedInvitationsStatistics`](/api/objects/pb-accepted-invitations-statistics))

Information about users that has been accepted the invitation

#### `invitationsTokens` ([`[VerificationToken!]!`](/api/objects/verification-token))

Information about the invitations tokens.

#### `pbCycle` ([`PBCycle!`](/api/objects/pb-cycle))

PB Cycle information
