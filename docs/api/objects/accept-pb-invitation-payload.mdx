---
id: accept-pb-invitation-payload
title: AcceptPBInvitationPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated return type of AcceptPBInvitation

```graphql
type AcceptPBInvitationPayload {
  clientMutationId: String
  pbCycle: PBCycle!
  status: ID!
  token: String!
  type: String!
}
```

### Fields

#### `clientMutationId` ([`String`](/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `pbCycle` ([`PBCycle!`](/api/objects/pb-cycle))

The PB cycle that the user is trying to join when accepting the invite.

#### `status` ([`ID!`](/api/scalars/id))

The result status. If no error detected, this value will be `success`.

#### `token` ([`String!`](/api/scalars/string))

#### `type` ([`String!`](/api/scalars/string))

The original invitation type. This type indicates the type of account that has been created for user in the PB cycle. Possible values are `ADMIN_INVITATION` or `PARTICIPANT_INVITATION`
