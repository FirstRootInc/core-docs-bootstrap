---
id: forgot-token-status
title: ForgotTokenStatus
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ForgotTokenStatus {
  isValid: Boolean!
  user: User
}
```

### Fields

#### `isValid` ([`Boolean!`](/docs/api/scalars/boolean))

#### `user` ([`User`](/docs/api/objects/user))
