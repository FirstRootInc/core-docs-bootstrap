---
id: ballot-result-proposal
title: BallotResultProposal
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type BallotResultProposal {
  ballotProposal: BallotProposal
  ballotResult: BallotResult
  createdAt: ISO8601Date!
  dislikesCount: Int!
  fundCount: Int!
  id: ID!
  likesCount: Int!
  neutralCount: Int!
  preSelected: Boolean!
  rank: Int!
  selected: Boolean!
  updatedAt: ISO8601Date!
}
```

### Fields

#### `ballotProposal` ([`BallotProposal`](/api/objects/ballot-proposal))

The ballot proposals which this result proposals descends from.

#### `ballotResult` ([`BallotResult`](/api/objects/ballot-result))

The ballot result which this result proposal belongs to.

#### `createdAt` ([`ISO8601Date!`](/api/scalars/iso-8601-date))

#### `dislikesCount` ([`Int!`](/api/scalars/int))

Total dislikes received by the ballot proposal.

#### `fundCount` ([`Int!`](/api/scalars/int))

Total votes for funding received by the ballot proposal.

#### `id` ([`ID!`](/api/scalars/id))

#### `likesCount` ([`Int!`](/api/scalars/int))

Total likes received by the ballot proposal.

#### `neutralCount` ([`Int!`](/api/scalars/int))

Total neutral reactions received by the ballot proposal.

#### `preSelected` ([`Boolean!`](/api/scalars/boolean))

Indicates if this result proposals has been selected by the system to be implemented.

#### `rank` ([`Int!`](/api/scalars/int))

The ranking position and suggested order for the result proposal.

#### `selected` ([`Boolean!`](/api/scalars/boolean))

Indicates if this result proposals has been selected by admin to be implemented.

#### `updatedAt` ([`ISO8601Date!`](/api/scalars/iso-8601-date))
