---
id: react-to-proposal-payload
title: ReactToProposalPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated return type of ReactToProposal

```graphql
type ReactToProposalPayload {
  clientMutationId: String
  proposalId: ID!
  reactionId: ID!
}
```

### Fields

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `proposalId` ([`ID!`](/docs/api/scalars/id))

#### `reactionId` ([`ID!`](/docs/api/scalars/id))
