---
id: join-pb-cycle-with-existing-account-payload
title: JoinPBCycleWithExistingAccountPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated return type of JoinPBCycleWithExistingAccount

```graphql
type JoinPBCycleWithExistingAccountPayload {
  clientMutationId: String
  pbCycle: PBCycle
  status: String!
  token: String!
  type: String!
}
```

### Fields

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `pbCycle` ([`PBCycle`](/docs/api/objects/pb-cycle))

PB cycle requested to join with the invite token.

#### `status` ([`String!`](/docs/api/scalars/string))

Status of the request to join the PB cycle.

#### `token` ([`String!`](/docs/api/scalars/string))

#### `type` ([`String!`](/docs/api/scalars/string))

This type indicates the type of account that the user can accept when joining the PB cycle. Possible values are `ADMIN_INVITATION` or `PARTICIPANT_INVITATION`
