---
id: signed-url-response
title: SignedUrlResponse
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SignedUrlResponse {
  errors: [String!]
  signedUrl: SignedUrl
}
```

### Fields

#### `errors` ([`[String!]`](/docs/api/scalars/string))

Errors information when process fails

#### `signedUrl` ([`SignedUrl`](/docs/api/objects/signed-url))

Signed URL object when process is correct
