---
id: ballot-result
title: BallotResult
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type BallotResult {
  ballot: Ballot!
  ballotResultProposals: [BallotResultProposal!]!
  budget: Decimal!
  createdAt: ISO8601DateTime!
  id: ID!
  isRatified: Boolean!
  participantsCount: Int!
  pbCycle: PBCycle!
  ratifiedBy: Party
  remainingBudget: Decimal!
  statistics: ResultsStatistics!
  submittedAt: ISO8601DateTime
  updatedAt: ISO8601DateTime!
}
```

### Fields

#### `ballot` ([`Ballot!`](/api/objects/ballot))

#### `ballotResultProposals` ([`[BallotResultProposal!]!`](/api/objects/ballot-result-proposal))

List of proposals ratified by this ballot results

#### `budget` ([`Decimal!`](/api/scalars/decimal))

The budget assigned to the PB Cycle

#### `createdAt` ([`ISO8601DateTime!`](/api/scalars/iso-8601-date-time))

#### `id` ([`ID!`](/api/scalars/id))

#### `isRatified` ([`Boolean!`](/api/scalars/boolean))

Indicates if results have been ratified by an admin.

#### `participantsCount` ([`Int!`](/api/scalars/int))

The total number of participants in this PB Cycle

#### `pbCycle` ([`PBCycle!`](/api/objects/pb-cycle))

#### `ratifiedBy` ([`Party`](/api/objects/party))

The party which ratified the ballot results.

#### `remainingBudget` ([`Decimal!`](/api/scalars/decimal))

The budget assigned to the PB Cycle

#### `statistics` ([`ResultsStatistics!`](/api/objects/results-statistics))

Totals and sums of all reactions and interactions

#### `submittedAt` ([`ISO8601DateTime`](/api/scalars/iso-8601-date-time))

#### `updatedAt` ([`ISO8601DateTime!`](/api/scalars/iso-8601-date-time))
