---
id: deselect-voting-result-input
title: DeselectVotingResultInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of DeselectVotingResult

```graphql
type DeselectVotingResultInput {
  ballotResultsId: ID!
  proposalId: ID!
  clientMutationId: String
}
```

### Fields

#### `ballotResultsId` ([`ID!`](/docs/api/scalars/id))

The ballot results ID in which the proposals is included

#### `proposalId` ([`ID!`](/docs/api/scalars/id))

The ballot result proposal ID to be deselected

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
