---
id: react-to-proposal-input
title: ReactToProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of ReactToProposal

```graphql
type ReactToProposalInput {
  proposalId: String!
  reaction: ReactionsEnum!
  clientMutationId: String
}
```

### Fields

#### `proposalId` ([`String!`](/docs/api/scalars/string))

The proposal id that will be affected by reaction

#### `reaction` ([`ReactionsEnum!`](/docs/api/enums/reactions-enum))

The user reaction, for instance LIKE or DISLIKE

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
