---
id: react-to-party-ballot-proposal-input
title: ReactToPartyBallotProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of ReactToPartyBallotProposal

```graphql
type ReactToPartyBallotProposalInput {
  partyBallotId: ID!
  proposalId: String!
  reaction: ReactionsEnum!
  clientMutationId: String
}
```

### Fields

#### `partyBallotId` ([`ID!`](/docs/api/scalars/id))

The party ballot ID which proposal belongs to

#### `proposalId` ([`String!`](/docs/api/scalars/string))

The party proposal id that will be affected by reaction

#### `reaction` ([`ReactionsEnum!`](/docs/api/enums/reactions-enum))

The party reaction, for instance LIKE or DISLIKE

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
