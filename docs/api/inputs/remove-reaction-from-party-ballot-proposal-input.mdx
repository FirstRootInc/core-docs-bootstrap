---
id: remove-reaction-from-party-ballot-proposal-input
title: RemoveReactionFromPartyBallotProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of RemoveReactionFromPartyBallotProposal

```graphql
type RemoveReactionFromPartyBallotProposalInput {
  partyBallotId: ID!
  proposalId: ID!
  clientMutationId: String
}
```

### Fields

#### `partyBallotId` ([`ID!`](/docs/api/scalars/id))

The party ballot ID which proposal belongs to

#### `proposalId` ([`ID!`](/docs/api/scalars/id))

The party proposal id which will be removed the reaction from

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
