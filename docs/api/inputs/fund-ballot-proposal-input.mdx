---
id: fund-ballot-proposal-input
title: FundBallotProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of FundBallotProposal

```graphql
type FundBallotProposalInput {
  partyBallotId: ID!
  proposalId: ID!
  fund: Boolean!
  clientMutationId: String
}
```

### Fields

#### `partyBallotId` ([`ID!`](/docs/api/scalars/id))

The party ballot ID which proposal belongs to

#### `proposalId` ([`ID!`](/docs/api/scalars/id))

The proposal ID in the party ballot

#### `fund` ([`Boolean!`](/docs/api/scalars/boolean))

Set the proposal funding to false or true

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
