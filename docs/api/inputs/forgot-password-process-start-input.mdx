---
id: forgot-password-process-start-input
title: ForgotPasswordProcessStartInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of ForgotPasswordProcessStart

```graphql
type ForgotPasswordProcessStartInput {
  email: Email!
  languageCode: String
  clientMutationId: String
}
```

### Fields

#### `email` ([`Email!`](/api/scalars/email))

User email. This email must exists otherwise an error will be returned.

#### `languageCode` ([`String`](/api/scalars/string))

Language code that will be used to send appropriate email message. Default to 'en'.

#### `clientMutationId` ([`String`](/api/scalars/string))

A unique identifier for the client performing the mutation.
