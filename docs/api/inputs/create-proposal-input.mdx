---
id: create-proposal-input
title: CreateProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of CreateProposal

```graphql
type CreateProposalInput {
  proposalId: ID
  name: String!
  description: String
  cost: Decimal
  thumbnail: String
  category: String!
  pbCycleId: ID!
  videoUrl: VideoUrl
  clientMutationId: String
}
```

### Fields

#### `proposalId` ([`ID`](/docs/api/scalars/id))

#### `name` ([`String!`](/docs/api/scalars/string))

A descriptive name for proposal.

#### `description` ([`String`](/docs/api/scalars/string))

Full length description for proposal.

#### `cost` ([`Decimal`](/docs/api/scalars/decimal))

Estimated cost for proposal. This cost is stored as decimal.

#### `thumbnail` ([`String`](/docs/api/scalars/string))

#### `category` ([`String!`](/docs/api/scalars/string))

The category which this proposal will be assigned to. If no value is provided the server will return an error.

#### `pbCycleId` ([`ID!`](/docs/api/scalars/id))

The PB Cycle `UUID` in which this proposal will be created.

#### `videoUrl` ([`VideoUrl`](/docs/api/scalars/video-url))

A video URL for proposal. The provided video must be a **[YouTube](https://youtube.com)** video URL otherwise the server will return an error.

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.
