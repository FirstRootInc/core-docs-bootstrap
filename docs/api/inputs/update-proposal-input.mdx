---
id: update-proposal-input
title: UpdateProposalInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Autogenerated input type of UpdateProposal

```graphql
type UpdateProposalInput {
  proposalId: ID!
  name: String
  description: String
  cost: Decimal
  categoryId: ID!
  videoUrl: VideoUrl
  clientMutationId: String
}
```

### Fields

#### `proposalId` ([`ID!`](/api/scalars/id))

The proposal ID to be updated.

#### `name` ([`String`](/api/scalars/string))

A descriptive name for proposal.

#### `description` ([`String`](/api/scalars/string))

Full length description for proposal.

#### `cost` ([`Decimal`](/api/scalars/decimal))

Estimated cost for proposal. This cost is stored as decimal.

#### `categoryId` ([`ID!`](/api/scalars/id))

The category which this proposal will be assigned to.

#### `videoUrl` ([`VideoUrl`](/api/scalars/video-url))

A video URL for proposal. The provided video must be a **[YouTube](https://youtube.com)** video URL otherwise the server will return an error.

#### `clientMutationId` ([`String`](/api/scalars/string))

A unique identifier for the client performing the mutation.
